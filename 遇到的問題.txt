1.TCP 傳輸細節與資料完整性處理

因為 TCP 是串流式的，所以伺服器傳的資料可能被分成好幾段送到 client，
client 一次 recv() 不一定收得完整。
為了避免資料只收到一半，我們在傳資料前，會先送一個資料長度，
client 收到長度之後，就持續收，直到收完指定大小的資料。
另外還要注意不同平台大小端問題，所以資料長度要用 htonl() 和 ntohl() 轉換，確保資料解讀正確。


---------------------------------------------------------------------
